/* ============================================================
   LAOT NIAGA — SELLER.JS v2 | Backward compat
   Logika seller-profile sudah inline di seller-profile.html
   ============================================================ */
function initSellerProfile() {
  console.info('[Laot Niaga] seller.js: logika sudah di seller-profile.html');
}
