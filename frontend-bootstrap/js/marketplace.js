/* ============================================================
   LAOT NIAGA — MARKETPLACE.JS v2 | Backward compat
   Logika marketplace sudah inline di marketplace.html
   ============================================================ */

// Stub: hanya dipakai jika ada halaman lama yang referensi file ini
function initMarketplace() {
  console.info('[Laot Niaga] marketplace.js: logika sudah di marketplace.html');
}